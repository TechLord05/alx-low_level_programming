#include "main.h"

/**
 * _isdigit - Checks for digit
 * @c: The variable to be checked
 * Return: 1 if c is a digit and 0 if otherwise
 */

int _isdigit(int c)
{
	if (c < '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
